spring:
  config:
    activate:
      on-profile: dev

image:
  upload-dir: /data/image-back-server/uploads/
  temp-retention-minutes: 180
  temp-cleanup-delay-ms: 3600000

attachment:
  internal-token: ${ATTACHMENT_INTERNAL_TOKEN}
